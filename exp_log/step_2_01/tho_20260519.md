class Processor 来自 qlib/data/dataset/processor.py:35
---
```python
class Processor(Serializable):
    def fit(self, df: pd.DataFrame = None):
        """
        learn data processing parameters

        Parameters
        ----------
        df : pd.DataFrame
            When we fit and process data with processor one by one. The fit function reiles on the output of previous
            processor, i.e. `df`.

        """

    @abc.abstractmethod
    def __call__(self, df: pd.DataFrame):
        """
        process the data

        NOTE: **The processor could change the content of `df` inplace !!!!! **
        User should keep a copy of data outside

        Parameters
        ----------
        df : pd.DataFrame
            The raw_df of handler or result from previous processor.
        """

    def is_for_infer(self) -> bool:
        """
        Is this processor usable for inference
        Some processors are not usable for inference.

        Returns
        -------
        bool:
            if it is usable for infenrece.
        """
        return True

    def readonly(self) -> bool:
        """
        Does the processor treat the input data readonly (i.e. does not write the input data) when processing

        Knowning the readonly information is helpful to the Handler to avoid uncessary copy
        """
        return False

    def config(self, **kwargs):
        attr_list = {"fit_start_time", "fit_end_time"}
        for k, v in kwargs.items():
            if k in attr_list and hasattr(self, k):
                setattr(self, k, v)

        for attr in attr_list:
            if attr in kwargs:
                kwargs.pop(attr)
        super().config(**kwargs)
```
---
class Processor 又用来实现 qlib/data/dataset/processor.py:322
---
```python
class CSZScoreNorm(Processor):
    """Cross Sectional ZScore Normalization"""

    def __init__(self, fields_group=None, method="zscore"):
        self.fields_group = fields_group
        if method == "zscore":
            self.zscore_func = zscore
        elif method == "robust":
            self.zscore_func = robust_zscore
        else:
            raise NotImplementedError(f"This type of input is not supported")

    def __call__(self, df):
        # try not modify original dataframe
        if not isinstance(self.fields_group, list):
            self.fields_group = [self.fields_group]
        # depress warning by references:
        # https://stackoverflow.com/questions/20625582/how-to-deal-with-settingwithcopywarning-in-pandas
        # https://pandas.pydata.org/pandas-docs/stable/user_guide/options.html#getting-and-setting-options
        with pd.option_context("mode.chained_assignment", None):
            for g in self.fields_group:
                cols = get_group_columns(df, g)
                df[cols] = df[cols].groupby("datetime", group_keys=False).apply(self.zscore_func)     # <-- 这里
        return df
```
---
最主要的是
```python
df[cols] = df[cols].groupby("datetime", group_keys=False).apply(self.zscore_func)
```
实现截面标准化实现
`groupby("datetime")` = 严格按单交易日截面,绝不跨日期。
**自检只需一句话:我的处理前面有没有 groupby(日期)?**
---
我们目前的实现
```python
def make_label(df: pd.DataFrame,
               n_periods: int = 5,
               price_col: str = "close",
               gap: int = 1,
               ) -> pd.Series:

    df = df.sort_values(['ticker', 'date'])
    by_inst = df.groupby('ticker')[price_col]

    enter = by_inst.shift(-gap)
    exit_ = by_inst.shift(-(gap + n_periods))

    return (exit_ / enter - 1).rename(f"label_{n_periods}d")
```
本身我们的也没有错,qlib的默认配置是
```python
# 默认处理链(handler.py:37-45)
_DEFAULT_LEARN_PROCESSORS = [          # 训练链
    {"class": "DropnaLabel"},
    {"class": "CSZScoreNorm", "kwargs": {"fields_group": "label"}},
]
_DEFAULT_INFER_PROCESSORS = [          # 推理/评估链
    {"class": "ProcessInf"},
    {"class": "ZScoreNorm"},           # 只动 feature,绝不碰 label
    {"class": "Fillna"},
]
```
默认用 CSZScoreNorm(截面 z-score),不是 CSRankNorm。它做 (label − 当天截面均值)/当天截面标准差——那个"减当天均值" 就是昨晚方向A说的"减掉大盘 beta",除标准差又把日波动归一.

暂时改进方向:
```python
df["zscore_rank"] = df.groundby("day", group_key = False).apply(*)
```
同时新写函数
```python
def _zscore(df: pd.Dataframe) -> pd.Dataframe:
    return
```


























